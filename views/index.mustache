<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>User Directory</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>User Directory</h1>
    <div class="results">
        {{#users}}
    <a  href="/listing/{{id}}">
        <div id="listing">
            <div class="avatar">
                <img src="{{avatar}}" alt="avatar">
            </div>
            <div class="name">
                {{name}}
            </div>

            <!-- <div class="education">
            {university}
        </div> -->
        <div class="job">{{#job}}
            <p> {{job}}, {{company}}  </p>
            {{/job}}
            {{^job}}
            <p id="available"> Available for hire</p>
            {{/job}}
            {{/users}}
        </div>
    </div>
    </a>
</div>
</body>
</html>
