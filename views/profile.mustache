{{#user}}
        <a href="#">
        <div class="userInfo">
            <div class="avatar">
                <img src="{{avatar}}" alt="avatar">
            </div>
            <h1>{{name}}</h1>
            <section>{{address.city}}}, {{address.state_or_province}} | {{#job}}
                 {{job}}, {{company}}
                {{/job}}
                {{^job}}
                 Available for hire
                {{/job}}</section>

        <table id="table">

            {{#education}}
        <tr>
            <td> <strong>Education</strong></td>
            <td>{{university}}</td>
        </tr>
        {{/education}}
        <tr>
            <td> <strong>Skills</strong></td>
            <td>{{skills}}</td>  <!-- not sure what this should be since skills is an array -->
        </tr>
    </table>
    </div>
    </a>
{{/users}}
